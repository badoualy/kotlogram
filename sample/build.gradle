apply plugin: 'java'

def compileLocal = false

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')

    if (compileLocal)
        compile project(':api')
    else
        compile 'com.github.badoualy:kotlogram:966cc99e8e'
}

repositories {
    maven { url "https://jitpack.io" }
}